import React from 'react';
import Layout from '../components/layout/Layout';
import Heading from '../components/library/headings/Heading';

const Speak = () => {
  return (
    <Layout>
      <Heading title="Want to speak at the meetup?" />
    </Layout>
  );
};

export default Speak;
